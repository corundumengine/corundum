// Translation unit anchor for the header-only engine_platform_null target.
// All real implementations are inline in
// engine/include/corundum/platform/null/*.hpp; this file exists only so the
// STATIC library has at least one source file.
